name: Matrix Configuration

on:
  push:
  workflow_dispatch:

jobs:
  deploy-on-ubuntu:
    runs-on: ubuntu-latest  # Fixed: Correct key name and value
    steps:
      - name: Echo Docker details
        run: docker info

      - name: Run Docker image
        run: docker run hello-world

  deploy-on-windows:
    runs-on: windows-latest  # Fixed: Added missing `runs-on` key
    steps:
      - name: Echo Docker details
        run: docker info

      - name: Run Docker image
        run: docker run hello-world